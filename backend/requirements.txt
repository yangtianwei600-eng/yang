# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.27

# 数据库
sqlalchemy>=2.0

# 配置 / 校验
pydantic>=2.6
pydantic-settings>=2.1

# 谷歌登录 (OAuth) + HTTP
authlib>=1.3
httpx>=0.27

# 会话签名 (SessionMiddleware 依赖)
itsdangerous>=2.1

# 表单解析 (OAuth 回调可能用到)
python-multipart>=0.0.9
